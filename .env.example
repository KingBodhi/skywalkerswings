# Skywalker Environment Configuration Template
# Copy this file to .env and fill in the required values

# Database Configuration
# Replace with your actual Postgres connection string (example below)
# DATABASE_URL="postgresql://skyfox_user:<password>@dpg-d3hdebu3jp1c73ffebmg-a.oregon-postgres.render.com/skyfox?sslmode=require&schema=public"
DATABASE_URL="postgresql://<user>:<password>@<host>/<database>?sslmode=require&schema=public"

# Authentication - REQUIRED FOR PRODUCTION
# Generate with: openssl rand -base64 64
SESSION_SECRET=""

# Stripe Payment Configuration
# Get these from your Stripe Dashboard
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Email Configuration (SMTP)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# Application URLs
# For development
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
# For production (set to your actual domain)
# NEXT_PUBLIC_BASE_URL="https://your-production-domain.com"

# Contact Form
CONTACT_RECEIVER_EMAIL=""

# Node Environment
NODE_ENV="development"

# Optional: Redis for production rate limiting
# REDIS_URL=""

# Optional: Monitoring and Logging
# LOG_LEVEL="info"
